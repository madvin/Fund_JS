function addSubstract(num1, num2, num3) {
    function addion(a,b) {
        const assemble = a + b;
        return assemble;
    }
    function substraction (a, b) {
        const sub = a - b;
        return sub;
    }
    const result = addion(num1, num2);
    const finalResult = substraction(result, num3)
    console.log(finalResult);

}
addSubstract(23,

    6,
    
    10)